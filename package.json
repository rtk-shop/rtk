{
  "name": "repo",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "turbo run dev",
    "dev:tg": "turbo run dev --filter=web-app --filter=@repo/ui",
    "dev:admin": "turbo run dev --filter=admin --filter=@repo/ui"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
