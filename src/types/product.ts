export const enum ProductTag {
  new,
  stock,
  top
}

export type P = {
  id: string
}

export const enum Gender {
  female,
  male,
  unisex
}

export const enum Category {
  backpack,
  bag,
  suitcase,
  wallet,
  other
}
